general:
  artifacts:
    - "target/ride-recorder-api-0.1.0.jar"
    
test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - mvn package
      - ./deploy_staging.sh
